name: domotica_pro
description: Domótica funcional
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  shared_preferences: ^2.2.2

  shared_preferences: ^2.3.0
  camera: ^0.11.0
  http: ^1.2.0          # ← nuevo

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
