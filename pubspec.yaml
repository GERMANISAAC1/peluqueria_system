name: domotica_pro
description: Aplicación de domótica profesional - Versión 2.0
publish_to: 'none'
version: 2.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  
  # UI
  cupertino_icons: ^1.0.6
  google_fonts: ^6.1.0
  
  # Persistencia
  shared_preferences: ^2.2.2
  
  # Networking
  http: ^1.1.0
  
  # Utilidades
  flutter_local_notifications: ^16.3.0
  intl: ^0.18.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
